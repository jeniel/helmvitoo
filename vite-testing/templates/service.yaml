apiVersion: v1
kind: Service
metadata:
  name: {{ include "vite-testing.fullname" . }}
  labels:
    {{- include "vite-testing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vite-testing.selectorLabels" . | nindent 4 }}
